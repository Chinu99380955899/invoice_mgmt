# Lean dev dependencies for Windows / quick-start.
# Mirrors requirements.txt minus the heavy OCR stack (paddlepaddle / paddleocr)
# which is hard to install on Windows + Python 3.11. The Challenger OCR agent
# falls back to its deterministic mock when paddleocr is not importable, so the
# full API + pipeline still runs end-to-end.

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Data validation
pydantic==2.7.4
pydantic-settings==2.3.4
email-validator==2.2.0

# Database
sqlalchemy==2.0.31
alembic==1.13.2
psycopg2-binary==2.9.9

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# Task queue
celery==5.4.0
redis==5.0.7
kombu==5.3.7

# Image / OCR  (paddle* intentionally omitted — mocks handle them)
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0
pdf2image==1.17.0
azure-ai-formrecognizer==3.3.3
azure-storage-blob==12.20.0

# Utilities
httpx==0.27.0
tenacity==8.4.2
python-dotenv==1.0.1
structlog==24.2.0
loguru==0.7.2

# Observability / resilience
prometheus-fastapi-instrumentator==7.0.0
pybreaker==1.2.0

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
factory-boy==3.3.0
